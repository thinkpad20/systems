class Foo {
	//this is a comment
	field int x, z;
	field int y; // this is another comment
	field string str;
	static int fuzz, theta;
	static Broda bb;
	/* this is another comment */
	// constructor Foo new(int _x, int _y) {
	// 	let x = _x;
	// 	let y = _y;
	// 	let str = "this is a test, mothafuzksdf $$";
	// 	return this;
	// }
	method int baz (int poop, int loopdeloop) {
		var int i, j;
		let poop = -345;
		let i = 1;
		let j = -1;
		if (x > y) {
			return i;
		}
		else {
			return j;
		}
	}
}